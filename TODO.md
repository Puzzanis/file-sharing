# 📋 План развития проекта (Roadmap)

Список задач и идей по улучшению сервиса обмена файлами.

## 🟢 Низкий приоритет (UI/UX улучшения)
- [ ] **Темная тема (Dark Mode)**: Добавить переключатель в интерфейс.
- [ ] **Анимация уведомлений**: Использовать библиотеку (напр. Animate.css) для плавного появления окна "Принять/Отклонить".
- [ ] **Иконки типов файлов**: Отображать разные иконки для `.jpg`, `.pdf`, `.zip` и т.д. в истории и уведомлениях.

## 🟡 Средний приоритет (Функционал)
- [ ] **История в браузере**: Создать отдельную страницу или модальное окно `/history`, где будут выводиться последние 20 записей из SQLite.
- [ ] **Текстовый чат**: Реализовать возможность отправки коротких сообщений пользователю перед или во время передачи файла.
- [x] **Отказ от файла**: Реализовать сигнал `decline`, чтобы отправитель видел статус "Отклонено получателем", а не висел в ожидании.
- [ ] **Уникальные ники**: Позволить пользователям менять стандартный `User-XXXX` на свой никнейм.

## 🟠 Высокий приоритет (Надежность и Безопасность)
- [ ] **Авторизация**: Создать таблицу `users` в БД и реализовать вход по логину/паролю.
- [ ] **Обработка обрывов**: Если WebSocket закрылся во время передачи, принудительно закрывать `io.Pipe` и очищать ресурсы.
- [ ] **Лимит размера**: Добавить проверку на стороне сервера, чтобы нельзя было начать передачу файла более, например, 10 ГБ (защита от спама).
- [ ] **Docker**: Написать `Dockerfile` и `docker-compose.yml` для развертывания одной командой.

## 🔴 Hardcore (Архитектура)
- [ ] **Докачка (Resumable Uploads)**: Перейти со стриминга на чанки (кусочки), чтобы можно было возобновить передачу после сбоя интернета.
- [ ] **WebRTC**: Попробовать реализовать чистый P2P между браузерами, используя сервер только для сигналов (полный уход от нагрузки на сервер).
- [ ] **Авто-сертификаты**: Интеграция с Let's Encrypt (`autocert`) для автоматического получения реальных SSL-сертификатов.

---
*Проект находится в активной разработке. Предложения приветствуются!*
