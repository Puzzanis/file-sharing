markdown
# 🚀 Go File Sharing P2P (Secure Stream)

Легкий и быстрый сервис для обмена файлами между пользователями в реальном времени. Работает по принципу "Pipe" (труба): данные передаются напрямую от отправителя к получателю через оперативную память сервера, не сохраняясь на диск.

## ✨ Особенности

*   **P2P-стриминг**: Передача файлов любого размера (хоть 100 ГБ) без нагрузки на HDD/SSD сервера.
*   **Безопасность**: Поддержка **HTTPS** и **WSS** (WebSockets Secure) с самоподписанными сертификатами.
*   **Уведомления**: Система подтверждения "Принять/Отклонить" и звуковой сигнал при получении.
*   **Прогресс-бар**: Визуальное отображение статуса передачи на обеих сторонах.
*   **Чистая архитектура**: Логика разбита по пакетам (`internal/`), фронтенд отделен от бэкенда (`ui/`).

## 🛠 Технологии

*   **Backend**: Go (Golang) + `gorilla/websocket`
*   **Frontend**: Native JS, HTML5, CSS3
*   **Security**: TLS (HTTPS/WSS)

## 📦 Структура проекта

```text
file-sharing/
├── main.go             # Точка входа и настройка маршрутов
├── internal/           # Внутренняя логика проекта
│   ├── handlers/       # Обработчики HTTP и WebSocket
│   ├── hub/            # Управление списком пользователей
│   └── transfer/       # Логика стриминга (io.Pipe)
├── ui/                 # Фронтенд (HTML, CSS, JS)
└── scripts/            # Вспомогательные скрипты (генерация TLS)
```

## 🛠 Инструкция по запуску

Для работы приложения необходим установленный **Go 1.18+**.

### 1. Подготовка окружения
Склонируйте проект и перейдите в его директорию:
```bash
cd file-sharing
```
### 2. Генерация SSL-сертификатов (обязательно для HTTPS)
```bash
go run scripts/generate_cert.go
```
### 3. Установка зависимостей
```bash
go mod tidy
```
### 4. Запуск сервера
```bash
go run main.go
```
После этого терминал напишет: 🚀 Secure Server запущен: https://localhost:8080

## 📖 Как пользоваться (Инструкция)
Приложение позволяет передавать файлы между вкладками одного браузера или разными устройствами в одной сети.
1. Вход в приложение:
    1. Откройте в браузере адрес https://localhost:8080.
    2. Важно: Браузер предупредит о "Небезопасном соединении" (из-за самоподписанного сертификата). Нажмите "Дополнительно" -> "Перейти на сайт (небезопасно)".
    3. Откройте ту же страницу во второй вкладке или другом браузере (например, в режиме инкогнито).
2. Выбор адресата:
    1. В списке "Люди в сети" вы увидите ID других подключенных пользователей (например, User-52282).
    2. Напротив нужного пользователя нажмите синюю кнопку "Файл".
3. Передача:
    1. Выберите любой файл на вашем компьютере.
    2. У получателя во второй вкладке появится уведомление со звуком и кнопками "Принять / Отмена".
    3. Нажмите "Принять".
4. Контроль:
    1. Отправитель увидит прогресс-бар загрузки файла в сеть.
    2. Получатель увидит статус скачивания в интерфейсе и стандартную загрузку файла в браузере.
    3. По завершении оба пользователя получат уведомление: "✅ Передача завершена!".